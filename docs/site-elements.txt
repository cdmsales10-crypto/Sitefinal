Site Elements Inventory - Card Detailing Mart

Overview:
This document lists every page, each section on that page, and every UI element inside those sections (type, visible text, key CSS classes, icons, links, and behaviors) based on current source files.

---

HomePage (src/pages/HomePage.tsx)
- Root: <div className="min-h-screen bg-white text-black">

1) Hero Section
  - Container: <section className="relative bg-gradient-organic py-16 ... text-black">
  - Background image: absolute div with inline background-image (pexels URL), opacity 10%.
  - Logo: <img src="/cmdlogo.svg" className="w-60 h-60 ... animate-float" />
  - Title: <h1> "Card Detailing Mart" (responsive heading classes: text-3xl..text-7xl)
  - Subtitle: <p> "Nourish Your Wellbeing"
  - Description: <p> marketing paragraph (max-w-3xl)
  - CTA Button: <button className="btn-gold inline-block ...">Explore Products</button>
    - Behavior: scrolls to #products via document.getElementById(...).scrollIntoView({behavior:'smooth'})

2) Key Benefits Section
  - Container: <section className="py-12 ... bg-white text-black">
  - Heading: "Key Benefits"
  - Grid: 4 cards (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4)
  - Card (repeated 4x):
    - Wrapper: div.card-premium p-6 ... text-black
    - Icon container: round bg-sage, contains lucide icon (Shield/Heart/Sparkles/Leaf) with classes like text-black
    - Heading: <h3> (benefit title)
    - Paragraph: short description

3) Products Section (id="products")
  - Heading: "Our Products"
  - Grid mapping: {PRODUCTS.map(...) => <ProductCard />}
  - ProductCard rendered elements (see ProductCard section)

4) Floating Cart Button (conditional when hasItems)
  - Fixed button bottom-right
  - Classes: uses gradient utilities (bg-gradient-to-br from-herbal to-herbal-dark ...), text-white, rounded-full, shadow-premium, animate-pulse-glow
  - Contents: <ShoppingCart /> icon + text (responsive: "Check Cart" / "Cart")
  - Behavior: onClick => onNavigate('cart')

5) CTA Section
  - Background: bg-gradient-to-br from-herbal to-forest (hero-like)
  - Heading: "Join the Card Detailing Mart Family"
  - Paragraph: marketing copy
  - CTA Button: white button with black text
    - onClick => onNavigate('contact')

---

AboutPage (src/pages/AboutPage.tsx)
- Root: <div className="min-h-screen bg-white">

1) Hero Section
  - Container: bg-gradient-organic
  - Heading: "About Card Detailing Mart" (big display)
  - Intro paragraph (text-herbal)

2) Story Section
  - Paragraphs describing mission & product/service overview

3) Values Section
  - Heading: "Our Values"
  - Grid: 4 value cards
  - Each card:
    - .card-premium wrapper
    - Icon in round bg-sage (icons: Leaf, Target, Heart, Award)
    - Title (e.g., "100% Organic", "Quality First")
    - Description paragraph

4) Our Story Section
  - Heading: "Our Story"
  - Several paragraphs with company history and services (detailing services list: exterior wash, interior deep-cleaning, paint protection, ceramic coatings)

5) CTA Section
  - Background: bg-gradient-to-br from-herbal to-forest
  - Heading/paragraph inviting users to join community

---

ContactPage (src/pages/ContactPage.tsx)
- Root: <div className="min-h-screen bg-white">

1) Hero Section
  - Heading: "Get in Touch"
  - Short intro line

2) Contact Information Section (two-column grid)
  - Left column: Contact cards
    - Phone Card
      - Icon: <Phone /> inside w-14 h-14 bg-sage
      - Title: "Phone"
      - Link: <a href="tel:9000805105">9000805105</a>
    - Email Card
      - Icon: <Mail />
      - Title: "Email"
      - Link: <a href="mailto:info@cdmonline.in">info@cdmonline.in</a>
    - Address Card
      - Icon: <MapPin />
      - Title: "Address"
      - Text: multiline address (CDM (Car Detailing Mart) · Opposite Mahatma Gandhi Bus Stand (MGBS) · Hyderabad, Telangana-500024)
    - WhatsApp Card
      - Wrapper: gradient card (bg-gradient-to-br from-herbal to-herbal-dark), text-white
      - Icon: <MessageCircle />
      - Title: "WhatsApp"
      - Description: short paragraph
      - CTA: <a href="https://wa.me/919000805105" className="inline-block bg-white text-herbal ...">Chat on WhatsApp</a>
      - Behavior: opens WhatsApp in new tab
  - Right column:
    - Business Hours card (card-premium)
      - Rows: Monday-Friday, Saturday, Sunday (times listed)
    - Quick Response box (bg-sage border-herbal/30) descriptive text

3) CTA Section
  - Heading: "Have Questions?"
  - CTA anchor button: <a href="https://wa.me/919000805105" className="inline-flex ... bg-gradient-to-br from-herbal to-herbal-dark ...">Start a Conversation</a>
  - Contains MessageCircle icon

---

CartPage (src/pages/CartPage.tsx)
- Root: <div className="min-h-screen bg-white ...">

1) Empty Cart View (when cart.length === 0)
  - Centered block
  - Icon: <ShoppingBag /> large
  - Heading: "Your Cart is Empty"
  - Paragraph: "Add some products to get started!"
  - CTA: <button className="btn-primary ...">Browse Products</button>

2) Shopping Cart (list, when items present)
  - Page heading: "Shopping Cart"
  - List wrapper: .card-premium with child item blocks
  - Each cart item block:
    - Container: p-4 sm:p-6 ... border-b-2 border-sage (except last)
    - Product image: <img src={item.product.image} ... className="w-full sm:w-24 md:w-32 h-32 object-cover rounded-xl" />
    - Product title: <h3>{item.product.name}</h3>
    - Size: text showing selected size (span.text-herbal)
    - Remove button: <button aria-label="Remove item"> with <Trash2 /> icon
      - Classes: text-red-500 hover:text-red-700 p-2 ... rounded-xl
      - Behavior: removeFromCart(item.product.id, item.size)
    - Quantity controls:
      - Decrease button: <button onClick updateQuantity ...> with <Minus />
      - Quantity display: <span>{item.quantity}</span>
      - Increase button: <button onClick updateQuantity ...> with <Plus />
      - These controls are in a bg-sage rounded container
    - Price display: per-item and total price for that line

  - Summary card (.card-premium): Subtotal and Total rows

  - Checkout flow (showCheckout state)
    - Modal: fixed full-screen overlay with semi-transparent backdrop
    - Form fields inside .card-premium: Name, Phone, Full address (textarea), Pincode
      - Inputs: className uses border-red when errors else border-sage, bg-white
      - Error messages: span.text-red-500
    - Buttons: Cancel (bg-sage) and Confirm & Send to WhatsApp (gradient button)
      - Confirm behavior: validates fields, composes WhatsApp message lines (includes order details), opens wa.me link with encoded message, clears cart, resets state, navigates to home

  - Bottom actions (always visible): Continue Shopping (bg-sage) and Finalize Order via WhatsApp (gradient)
    - Finalize shows MessageCircle icon and triggers handleSubmitOrder

---

ProductCard (src/components/ProductCard.tsx)
- Props: { product: Product }
- Root: div.card-premium overflow-hidden

1) Media area
  - .h-56.. group with <img src={product.image} alt={product.name} className="object-cover group-hover:scale-110" />
  - Overlay div: absolute inset-0 bg-gradient-to-t from-forest/20 to-transparent opacity change on hover

2) Content area
  - Title: <h3>{product.name}</h3>
  - Description: <p>{product.description}</p>

3) Key Benefits
  - Heading: "Key Benefits:"
  - Badges: each benefit rendered as <span className="text-xs bg-sage text-herbal px-3 py-1.5 rounded-full ...">{benefit}</span>

4) Size selection
  - Two buttons (250g, 500g)
  - Selected variant: 'border-herbal bg-herbal text-white shadow-glow'
  - Unselected: 'border-sage hover:border-herbal bg-white'

5) Quantity control
  - Minus button (decrease), numeric input (value), plus button (increase)
  - Buttons styled white with border-herbal and hover states

6) Add to Cart button
  - Shows either "Added to Cart" with Check icon (bg-gold) or "Add to Cart" with Plus icon (bg-herbal)
  - Behavior: addToCart(product, selectedSize, quantity, price), shows temporary added state

---

WhatsAppButton (src/components/WhatsAppButton.tsx)
- Anchor tag fixed bottom-left linking to wa.me with prefilled message
- Classes: gradient background, rounded-full, shadow-premium, transforms on hover (scale 110)
- Contents: <MessageCircle /> icon
- Hover tooltip: absolute span with group-hover opacity that says "Chat with us!" (hidden on small screens)
- Behavior: opens WhatsApp in new tab

---

Header (src/components/Header.tsx)
- Props: { currentPage: string; onNavigate: (page:string) => void }
- Root: <header className="bg-white shadow-sm sticky top-0 z-50 border-b border-sage"> (note: classes may vary depending on recent edits)

1) Brand area (click navigates to 'home')
  - Logo image: /cmdlogo.svg (w-12 h-12)
  - Brand text: "Card Detailing Mart" (text-lg..font-bold)
  - Subtitle: "Professional Car Detailing"

2) Desktop Navigation (hidden on small screens)
  - Buttons: Home, About Us, Contact Us, Policies
    - Each is a <button> that calls handleNavigate(page)
    - Active page uses a different class in template (e.g., border-b-2 and herbal color)
    - Hover uses herbal color (site's previous token names present)
  - Cart button: circular button with ShoppingCart icon, shows badge count when totalItems > 0

3) Mobile controls
  - Cart button (small)
  - Menu toggle (Menu/X icons) toggles mobileMenuOpen

4) Mobile nav when open
  - Stacked buttons for pages; active page uses bg-sage / text-herbal in current template (may change)

Notes: header contains many theme class names (sage, herbal, forest) that may be targeted for palette replacement.

---

Footer (src/components/Footer.tsx)
- Props: { onNavigate?: (page: string) => void }
- Root: <footer className="bg-forest text-sage">

1) Left column: Brand
  - Small logo image (/cmdlogo.svg) and brand name text
  - Short descriptive paragraph

2) Middle column: Quick Links
  - List of buttons: Home, About Us, Contact Us, Policies (each triggers go(page) helper)
  - Buttons have hover:text-gold and are inline-block

3) Right column: Contact Us
  - Heading: "Contact Us"
  - Phone: <Phone /> icon (text-gold) + <a href="tel:9000805105"> link
  - Email: <Mail /> + mailto link
  - Address: <MapPin /> + address text

4) Footer bottom bar
  - border-top border-herbal-dark, centered copyright text (text-sage-light)

---

Context & App wiring (summary)
- `src/context/CartContext.tsx`: provides cart state, add/remove/update functions and getTotalPrice/getTotalItems. Used across pages/components.
- `src/App.tsx` / `main.tsx`: route/navigation wiring via `onNavigate` props; Header and Footer are mounted globally.

---

Styling notes (observed usage)
- Many classes reference legacy tokens: herbal, forest, sage, gold, cream, etc. Global CSS (`src/index.css`) defines utilities like .btn-primary, .card-premium, .input-premium, and some gradient-to-solid helpers (bg-gradient-organic).
- Several elements use gradient utilities (bg-gradient-to-br from-herbal to-herbal-dark) — these appear frequently on CTAs and floating buttons.
- Icons are lucide-react components with class-based sizing and color.

---

Potential actionables (if you want a follow-up)
- Replace all legacy color tokens (sage, herbal, forest, gold, cream) with the strict CDM palette tokens (black, #0F0F0F, #C1271A, #FBCB2C, #FFFFFF, #B3B3B3, #2A2A2A) per your design rules.
- Remove gradient utilities and substitute solid backgrounds for CTAs and floating buttons.
- Run a repo-wide search/replace to update class usage consistently.

---

Generated from source files:
- src/pages/HomePage.tsx
- src/pages/AboutPage.tsx
- src/pages/ContactPage.tsx
- src/pages/CartPage.tsx
- src/pages/PoliciesPage.tsx (not expanded here: similar structure to content pages; open file if you want a full breakdown)
- src/components/Header.tsx
- src/components/Footer.tsx
- src/components/ProductCard.tsx
- src/components/WhatsAppButton.tsx

End of inventory.
